[project]
name = "omni-api"
version = "0.1.0"
description = "Deterministic payload transformer MVP"
requires-python = ">=3.11"
dependencies = [
  "pytest>=8.0",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
testpaths = ["tests"]
